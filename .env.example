# Shelf — Environment Variables
#
# Copy this file to .env and fill in values:
#   cp .env.example .env
#
# This project works out of the box with zero configuration.
# These variables are optional overrides for production deployment.

# Rails
RAILS_ENV=development
SECRET_KEY_BASE=         # Override Rails secret (auto-generated in dev/test)

# Database (SQLite by default — no config needed)
# DATABASE_URL=          # Override for PostgreSQL in production

# Server
PORT=3000                # Puma port
WEB_CONCURRENCY=2        # Puma worker count
